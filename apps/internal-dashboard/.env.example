# Core API (server-side proxy)
#
# The Internal Dashboard proxies same-origin `/api/v1/*` requests to the Core API.
# These variables MUST be server-only (do not use NEXT_PUBLIC_ prefix).
CORE_API_BASE_URL="http://localhost:3000"
INTERNAL_API_KEY="your-internal-api-key-here"
